<!DOCTYPE html>
<html lang="en">
<head>
  {% set siteTitle = (title or (homeInfo.event.title ~ ' - ' ~ homeInfo.event.subtitle)) %}
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{ siteTitle }}</title>

  <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">

  {# Bootstrap CSS (keep if you're already bundling locally; otherwise CDN) #}
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  >

  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">

  {# Your existing stylesheet (preserves background/design) #}
  <link href="/assets/css/style.css" rel="stylesheet">

  {# Description #}
  <meta name="description" content="{{ description or homeInfo.event.meta_description }}">

  {# Social images: prefer page-level fields; fallback to data #}
  {% set preview = page.preview_image or preview_image or homeInfo.event.preview_image %}
  {% set pageUrl = page.url or homeInfo.event.url %}

  {# Open Graph #}
  <meta property="og:title" content="{{ siteTitle }}">
  <meta property="og:description" content="{{ description or homeInfo.event.meta_description }}">
  <meta property="og:image" content="{{ preview }}">
  <meta property="og:url" content="{{ pageUrl }}">
  <meta property="og:type" content="website">

  {# Twitter Card #}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ siteTitle }}">
  <meta name="twitter:description" content="{{ description or homeInfo.event.meta_description }}">
  <meta name="twitter:image" content="{{ preview }}">
</head>
<body>

  {# Main content #}
  {{ content | safe }}

  {# Bootstrap JS (optional if needed) #}
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>

</body>
</html>
